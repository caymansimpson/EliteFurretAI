# Next steps

1.  **"Can't switch: You can't switch to an active Pok"** - Here the engine attempted `/choose move uturn 2, switch Farigiraf` while the request clearly dictates that `p1: Farigiraf` is already copied and in `Ditto`'s slot (offset 1) out! Wait, looking closer: `p1: Farigiraf` is listed as `active=False` in `Side Pokemon` but the agent attempted to switch it while `Ditto` had `transformed` into `p2a: Farigiraf`. Oh, actually the target `switch Farigiraf` is ambiguous here because both players had Farigiraf, but `Ditto` transformed into either. I will need to look deeper into `fast_action_mask.py:get_valid_switch_indexes_for_request_move` to see why Farigiraf was even enabled in the switch mask, especially given the `['active=False', hp=1.0]` property in Ditto's slot.

2. **"Can't move: Surging Strikes needs a target -> /choose move surgingstrikes..."**. Here, Urshifu attempted `surgingstrikes` on `slot 0` because it was forced/had no targets or something similar. Wait. Surgingstrikes is `disabled=True` in the request entirely because Urshifu is `item=choicescarf`, it was previously locked into `Coaching`, so only `coaching` is `disabled=False`. Yet, the agent literally selected `surgingstrikes`, bypassing the mask entirely! What's even stranger is that the companion slot 1 was disabled, and so `choicemove surgingstrikes, switch Iron Hands` was formatted. `Iron Hands` is fainted. This means somehow both the move mask, item constraint, and the switch mask failed concurrently. Oh, wait: `Iron Hands` is fainted, `Miraidon` is fainted! This was a case where the battle had exactly 1 active alive pokemon. Our mask or decoding fallback is probably failing massively here!